version = 2
template = "langchain/markdown-messages"
name = "{{ cookiecutter.project_name }}"
description = "LangChain chat application that streams messages to a markdown-rendering browser frontend."
env_file = ".env"
guide = "README.md"

[tools]
required = ["uv", "node", "npm"]

[paths]
required = ["frontend/package.json", "frontend/node_modules"]

[env.AGENTSEEK_API_KEY]
required = true
description = "Model provider API key. OPENAI_API_KEY is accepted by the LangChain bridge."
aliases = ["OPENAI_API_KEY"]

[env.AGENTSEEK_API_BASE]
required = false
description = "Optional OpenAI-compatible base URL. OPENAI_API_BASE is accepted by the LangChain bridge."
aliases = ["OPENAI_API_BASE"]

[env.AGENTSEEK_MODEL]
required = true
description = "Chat model id passed to LangChain init_chat_model. OPENAI_MODEL is accepted as an alias."
aliases = ["OPENAI_MODEL"]

[services.backend]
name = "AgentSeek API"
url = "http://127.0.0.1:{{ cookiecutter.langgraph_port }}"
kind = "api"
display = "advanced"
primary = false
description = "AgentSeek API runtime that runs the LangChain chat agent."
tech = "agentseek-api"
links = { docs = "https://github.com/ob-labs/agentseek-api", api_docs = "http://127.0.0.1:{{ cookiecutter.langgraph_port }}/docs", studio = "https://smith.langchain.com/studio/?baseUrl=http://127.0.0.1:{{ cookiecutter.langgraph_port }}" }

[services.frontend]
name = "Markdown chat application"
url = "http://127.0.0.1:{{ cookiecutter.frontend_port }}"
kind = "web"
display = "default"
primary = true
description = "Browser application that streams chat messages and renders markdown responses."
tech = "vite"
links = { docs = "https://vite.dev/guide/" }

[processes.backend]
command = ["uv", "run", "agentseek-api", "dev", "--port", "{{ cookiecutter.langgraph_port }}"]

[processes.frontend]
command = ["npm", "run", "dev"]
cwd = "frontend"

[checks.backend]
type = "http"
target = "http://127.0.0.1:{{ cookiecutter.langgraph_port }}/health"
service = "backend"
timeout = 2
attempts = 3

[checks.frontend]
type = "http"
target = "http://127.0.0.1:{{ cookiecutter.frontend_port }}"
timeout = 2
attempts = 3

[tasks.sync]
description = "Install Python dependencies."
command = ["uv", "sync"]

[tasks.frontend]
description = "Install frontend dependencies."
command = ["npm", "install", "--prefix", "frontend"]
