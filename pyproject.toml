[project]
name = "agentseek-templates"
version = "0.1.2"
description = "Versioned lifecycle-v2 template catalog for AgentSeek"
requires-python = ">=3.12,<3.14"
dependencies = []

[dependency-groups]
dev = [
    "agentseek @ git+https://github.com/ob-labs/agentseek.git@2d91d5e8ab1b8eabae74c95057a5a0139e9b4abc",
    "cookiecutter>=2.6.0,<3",
    "pytest>=8.4,<10",
    "ruff>=0.12,<1",
]

[tool.uv]
package = false

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
extend-exclude = ["templates"]
line-length = 120
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]
